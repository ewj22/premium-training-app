/**
 * This file is a placeholder. Once your Supabase project is live, generate
 * real types with:
 *
 *   supabase gen types typescript --project-id <your-project-id> \
 *     > src/shared/types/database.types.ts
 *
 * Do this after every migration in packages/database/migrations. Everything
 * downstream (queries, hooks, forms) depends on this file being current —
 * treat it as generated code, never hand-edit it.
 */
export type Database = any;
